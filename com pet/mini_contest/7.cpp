#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long n, m, s, e, u, v, w, q;
    cin >> n >> m >> s >> e;
    for(int i = 0; i < m; i++) {
        cin >> u >> v >> w;
        adj[u].push_back({v, w});
    }
    cin >> q;
    
    for 

    return 0;
}
